var showButton = document.getElementsByClassName('button');

for (var i = 0; i <= showButton.length; i++) {
		
	 	showButton.innerText = 'The text inside buttons 1 2 3';
		alert(showButton.innerText);
	}